--- lib/sketchgit/realtime/wsClient.ts
+++ lib/sketchgit/realtime/wsClient.ts
@@ -217,7 +217,7 @@
       return;
     }

-    if (process.env.NODE_ENV === 'development') {
+    if (typeof process !== "undefined" && process.env.NODE_ENV === 'development') {
       logger.warn(`[WsClient] Reconnecting in ${delayMs}ms (attempt ${this.reconnectAttempts})`);
     }
